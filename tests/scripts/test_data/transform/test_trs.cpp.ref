#include "test_trs.h"

int main()
{
	int i;
	A a;
	A* aa = new A();
	a.SetI3(10);
	a.SetPint(&i);
	a.SetI(a.GetC() + *a.GetPint());
	a.SetB(false);
	a.SetF(float(a.GetI3()));

	B b;
	b.SetA(a);
	b.SetSp1(b.GetSp2());
	return 0;
}
