#include "test_trs.h"

int main()
{
	A a;
	B b;
	int i;
	A* aa = new A();
	b.SetI(aa->GetI());
	a.SetPint(&i);
	a.SetI(a.GetC() + *a.GetPint());
	a.SetB(false);
	a.SetF(float(a.GetI3()));
	return 0;
}
