#include "test_encap.h"

int A::foo(){
	return m_i;
}

int main()
{
	int i;
	A a;
	A* aa = new A();
	a.SetI3(10);
	a.SetPint(&i);
	a.SetI(a.GetC() + *a.GetPint());
	a.m_b = false;
	a.SetF(float(a.GetI3()));

	B b;
	C* pc;
	b.SetA(a);
	b.SetC(pc);
	b.SetSp1(b.GetSp2());
	return 0;
}
